# Screenshots

Drop UI captures here so the root `README.md` can render them. Expected filenames:

- `login.png` — login page
- `register.png` — register page
- `chat.png` — main chat (mfe_chat) view
- `contacts.png` — contacts list (mfe_contacts)
- `friend-requests.png` — friend requests view
- `architecture.png` — optional system / MF architecture diagram

PNG, JPG, or WebP. ~1600px wide is plenty.
